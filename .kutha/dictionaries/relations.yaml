schema: kutha-harness-relations/v1
# Process-plane FF6 twin. Not ADR-050's six kinds. Not legal norms.
# Unknown relation → the process JSONL does not grow.
# Override path with KUTHA_HARNESS_RELATIONS_PATH.
# `allows` is reserved for H4 membership snapshots; do not drop it from tip.

relations:
  - status
  - high
  - low
  - checks
  - cargo
  - tenant
  - allows
