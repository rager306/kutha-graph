# Wave 1 acceptance

Created in U1. Checked in U6 (2026-08-17).

- [x] Template: U1 files exist; a card following the template can be listed in `matrix.md` by `id`
- [x] Inventory: all Wave-1 names listed; absent trees not scored `code`
- [x] Consensus: pack has 3 queries; ≤3 search calls documented; degrade path recorded if used
- [x] Cards: each closed card has five layers or `no niche`; scores + confidence present
- [x] Stubs: at least one `claim`/`intent` card if RuVector facades were seen — `ruvector-cypher-empty-success` is `confidence: code` on a facade (empty success is observed in source; idea kept per R8/AE2)
- [x] Raven: ≥1 Raven card with non-`code` confidence — `raven-in-db-ai-agents` (`spec`)
- [x] Rollup: `matrix.md` rows ⊆ card ids
- [x] Queue: mid-wave discoveries only in `wave-queue.md`

Dummy `example-capability-slug` removed from `cards/` and `matrix.md`.
