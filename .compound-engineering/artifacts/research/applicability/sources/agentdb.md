# AgentDB dossier (Wave 1)

No kernel tree. Scouted `vendor-source/ruflo/plugins/ruflo-agentdb` (README + ADR-0001). Unindexed in CBM.

## Extracted

| card | confidence | note |
|------|------------|------|
| agentdb-hierarchical-memory | spec | controller registry; graphAdapter disabled |
| agentdb-attestation-mutation-guard | spec | G7 attestation + mutationGuard |

## Channel

Plugin/docs only. Upgrade to `code` only if an AgentDB source tree is later inventoried.
