---
title: P0 LFTJ WAL Materializer Spike
type: research
date: 2026-08-18
artifact_contract: ce-unified-plan/v1
artifact_readiness: implementation-ready
product_contract_source: ce-plan-bootstrap
execution: code
---

# P0 LFTJ, WAL cousin, Materializer trait

## Goal Capsule

Advance the heartbeat: (1) leapfrog multiway intersection on CSR (ADR-041, not a new WCOJ), (2) framed durable log with truncated-tail recovery (Rocks WAL *cousin*, not Rocks-as-SoT), (3) `Materializer` build/unload for CSR (ADR-040). Status stays **Proposed** (not Accepted). No Cypher, HNSW, ABAC, commits.

## Key Decisions

- KD1. LFTJ = seekable iterators + leapfrog; unary/common-neighbor and triangle listing as tests.
- KD2. On-disk SoT is still the event log; WAL file is durability of that log (kTolerateCorruptedTail analog).
- KD3. Do not add `rocksdb` crate in this unit (compile/link tax); cousin is enough to close D010-4 *shape*.
- KD4. Materializer trait is the view contract; pack lifecycle stays 021.

## DoD

`cargo test --workspace` green. New tests: leapfrog, truncated WAL recover, CSR unload.
